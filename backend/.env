# Core
SECRET_KEY = 'django-insecure-1*kz&z=l!tdg&gyw7jrycd3*aa9p#%g@ivcn#=#3=mpf(b_$=('
DEBUG = True
ALLOWED_HOSTS=127.0.0.1,localhost

# CORS/Frontend
FRONTEND_BASE_URL=http://localhost:5173

# DB (for local dev you can omit; Django defaults to sqlite3)
DATABASE_URL=postgres://user:pass@localhost:5432/rentms

# Email (dev: console backend prints emails to terminal)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=noreply@rentms.local

# If using SMTP in dev/prod (comment out console backend above and use these)
EMAIL_HOST=smtp.sendgrid.net
EMAIL_HOST_USER=apikey
EMAIL_HOST_PASSWORD=SG.xxxxxx_your_real_key_xxxxxx
EMAIL_PORT=587
EMAIL_USE_TLS=True

# JWT (optional overrides)
ACCESS_TOKEN_LIFETIME_MIN=60
REFRESH_TOKEN_LIFETIME_DAYS=7
